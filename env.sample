# Database
WORDPRESS_DB_NAME=wordpress
WORDPRESS_DB_USER=wp_user
WORDPRESS_DB_PASSWORD=change_me
WORDPRESS_DB_ROOT_PASSWORD=change_me_root

# Redis
REDIS_PASSWORD=change_me_redis

# Elasticsearch
ELASTIC_PASSWORD=changeme

# Optional: WordPress site URL (used by some tools)
# WORDPRESS_CONFIG_EXTRA additional constants are defined in docker-compose.yml

